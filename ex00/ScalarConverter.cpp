#include "ScalarConverter.hpp"

static void	ScalarConverter::convert(std::string  const literal)
{
	struct number nb = {0, 0, 0, 0, 1, false, false, false, false};
	//chaine nulle
	//si len = 1
		// non digit printable char
	// special value 
	// if no point
		//format int
	// else
		// termine par f :type float, on le trime
		// sinon type double

}
